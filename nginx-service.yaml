apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    # Matches onto pods with labels of `app: nginx`
    app: nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
